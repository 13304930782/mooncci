import { useState } from 'react';import { useNavigate } from 'react-router-dom';import { useAuth } from '../context/AuthContext';
export default function RegisterPage(){const {register}=useAuth();const nav=useNavigate();const [f,sf]=useState({username:'',email:'',password:''});
return <div className='max-w-md mx-auto pt-32'><h1 className='text-2xl mb-4'>注册</h1><input className='w-full border p-2 mb-2' placeholder='用户名' onChange={e=>sf({...f,username:e.target.value})}/><input className='w-full border p-2 mb-2' placeholder='邮箱' onChange={e=>sf({...f,email:e.target.value})}/><input type='password' className='w-full border p-2 mb-2' placeholder='密码' onChange={e=>sf({...f,password:e.target.value})}/><button className='px-4 py-2 bg-blue-600 text-white' onClick={async()=>{await register(f.username,f.email,f.password);nav('/login');}}>注册</button></div>}
